[build]
  command = "npm run build"
  publish = "dist"

# Supabase connection for the build. The publishable/anon key is public by
# design (it ships in the browser bundle); data is protected by Row-Level
# Security. Setting them here means GitHub-connected builds work with no extra
# configuration in the Netlify UI.
[build.environment]
  VITE_SUPABASE_URL = "https://mzftplcyfotlzolqeapl.supabase.co"
  VITE_SUPABASE_ANON_KEY = "sb_publishable_oa96t2IYhNv_UE3nCx0LCw_s_amtTtO"
